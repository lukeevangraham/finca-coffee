{{!-- <div class="u-padding-y-medium"> --}}

  {{!-- <div class="row"> --}}



<main>

    {{#each sections}}
  {{#if (eq __component "section.hero")}}

    {{> sections/hero this}}
  {{/if}}
  
  {{#if (eq __component "section.heading-above-rich-text")}}
    {{> sections/heading_above_rich_text this}}
  {{/if}}

  {{#if (eq __component "section.heading-above-columns")}}
    {{> sections/heading_above_columns this}}
  {{/if}}

  {{#if (eq __component "section.testimonies")}}
    {{> sections/testimonials this}}
  {{/if}}

  {{#if (eq __component "section.heading-above-grid")}}
    {{> sections/features this}}
  {{/if}}

  {{#if (eq __component "section.fa-qs")}}
    {{> sections/faq this}}
  {{/if}}

  {{#if (eq __component "section.collage")}}
    {{> sections/collage this}}
  {{/if}}

  {{!-- Add other sections here --}}
{{/each}}

{{> sections/cta }}

  {{!-- </div> --}}
{{!-- </div> --}}

</main>