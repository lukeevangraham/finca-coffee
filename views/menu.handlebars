<main class="menu-page">
  {{!-- Section 1: Intro & Featured Items (Automatically gets $color-white) --}}
  <section class="section-menu">
    <div class="row">
      <div class="u-center-text u-margin-bottom-big">
        <h1 class="heading-primary">Our Menu</h1>
        <p class="menu-intro">Boutique coffee and handcrafted flavors curated for your event.</p>
      </div>
    </div>

    <div class="row">
      <div class="menu-grid">
        {{#each menuItems}}
          {{#if attributes.Is_featured}}
            <div class="menu-card {{#if attributes.Image.data}}menu-card--has-img{{else}}menu-card--text-only{{/if}}">
              {{#if attributes.Image.data}}
                <div class="menu-card__img-box">
                  <img src="{{attributes.Image.data.attributes.url}}" alt="{{attributes.Name}}" class="menu-card__img">
                </div>
              {{/if}}

              <div class="menu-card__content">
                <span class="menu-card__category">{{attributes.Category}}</span>
                <h3 class="heading-tertiary">{{attributes.Name}}</h3>
                <p class="menu-card__description">{{attributes.Description}}</p>
              </div>
            </div>
          {{/if}}
        {{/each}}
      </div>
    </div>
  </section>

  {{!-- Section 2: The Staples (Automatically gets zebra-striped to $color-quaternary!) --}}
  <section class="section-menu-staples">
    <div class="row">
      <div class="menu-list">
        <div class="u-center-text u-margin-bottom-big">
          <h2 class="heading-secondary">The Staples</h2>
        </div>

        <div class="menu-list__grid">
          {{#each menuItems}}
            {{#unless attributes.Is_featured}}
              {{#unless (eq attributes.Category "Add-on")}}
                <div class="menu-list__item">
                  <div class="menu-list__header">
                    <span class="menu-list__name">{{attributes.Name}}</span>
                  </div>
                  <p class="menu-list__desc">{{attributes.Description}}</p>
                </div>
              {{/unless}}
            {{/unless}}
          {{/each}}
        </div>
      </div>
    </div>
  </section>

  {{!-- Section 3: Custom Add-ons (Automatically gets $color-white) --}}
  <section class="section-menu-addons">
    <div class="row">
      <div class="menu-list">
        <div class="u-center-text u-margin-bottom-big">
          <h2 class="heading-secondary">Custom Add-ons</h2>
          <p class="menu-intro">Elevate your booking with curated upgrades and custom branding.</p>
        </div>

        <div class="menu-list__grid">
          {{#each menuItems}}
            {{#if (eq attributes.Category "Add-on")}}
              <div class="menu-list__item">
                <div class="menu-list__header menu-list__header--has-price">
                  <span class="menu-list__name">{{attributes.Name}}</span>
                  <span class="menu-list__price-badge">
                    ${{attributes.Price_starting_at}}
                    {{#if attributes.PricePerHour}}
                      <span class="menu-list__price-suffix">/ hr</span>
                    {{/if}}
                  </span>
                </div>
                <p class="menu-list__desc">{{attributes.Description}}</p>
              </div>
            {{/if}}
          {{/each}}
        </div>
      </div>
    </div>
  </section>

  {{!-- Section 4: Call to Action (Automatically gets zebra-striped to $color-quaternary!) --}}
  <section class="section-cta">
    <div class="row">
      <div class="cta u-center-text">
        <h2 class="heading-secondary u-margin-bottom-small">Ready to elevate your event?</h2>

        <p class="cta__text u-margin-bottom-medium">
          Love our flavors? Let's bring them to your celebration. 
          Inquire today to see if your date is available.
        </p>
        
        {{> UI/button 
            link="/inquire" 
            text="Inquire for Availability" 
            style="primary" 
            class="btn--animated"
        }}
      </div>
    </div>
  </section>
</main>